<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"

    android:id="@+id/card_view"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="3dp"
    android:layout_marginLeft="@dimen/_10sdp"
    android:layout_marginRight="@dimen/_10sdp"
    android:layout_marginTop="5dp"
    android:padding="3dp"
    card_view:cardElevation="2dp"
    card_view:cardMaxElevation="2dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="5dp">

        <TextView
            android:id="@+id/list_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:padding="5dp"
            android:text="%IngredientName%"
            android:textSize="16dp" />

        <ImageButton
            android:id="@+id/list_btDelete"
            android:layout_width="@dimen/_25sdp"
            android:layout_height="@dimen/_25sdp"
            android:background="@null"
            android:layout_alignParentRight="true"
            app:srcCompat="@drawable/cocktail_remove_icon"
            android:scaleType="fitCenter"
            android:padding="5dp"/>

        <ImageButton
            android:id="@+id/list_btEdit"
            android:layout_width="@dimen/_25sdp"
            android:layout_height="@dimen/_25sdp"
            android:layout_marginRight="2dp"
            android:background="@null"
            android:layout_toLeftOf="@+id/list_btDelete"
            app:srcCompat="@drawable/cocktail_edit_icon"
            android:scaleType="fitCenter"
            android:padding="5dp" />
    </RelativeLayout>

</androidx.cardview.widget.CardView>